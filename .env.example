# Frontend -> backend API base URL (or full endpoint) used by src/hooks/use-freebusy.ts
# If not set, the UI defaults to: http://localhost:8787/freebusy
VITE_FREEBUSY_API=http://localhost:8787/freebusy

# Optional: enable verbose iCal parse debugging in dev
# VITE_DEBUG_ICAL=true
